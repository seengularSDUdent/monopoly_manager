<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <Button
        android:id="@+id/join_button"
        android:layout_width="166dp"
        android:layout_height="55dp"
        android:backgroundTint="@color/join_button_background_color"
        android:fontFamily="@font/monopoly_inline"
        android:text="Join"
        android:textColor="@color/join_button_text_color"
        android:textSize="20sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.497"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/host_button"
        android:layout_width="166dp"
        android:layout_height="55dp"
        android:layout_marginTop="8dp"
        android:backgroundTint="@color/host_button_background_color"
        android:fontFamily="@font/monopoly_inline"
        android:text="Host"
        android:textSize="20sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.497"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/join_button" />

    <ImageButton
        android:id="@+id/host_config_button"
        android:layout_width="44dp"
        android:layout_height="44dp"
        android:layout_marginStart="8dp"
        android:adjustViewBounds="true"
        android:background="#007C1313"
        android:cropToPadding="true"
        android:scaleType="fitCenter"
        android:scaleX="0.78"
        android:scaleY="0.8"
        android:src="@drawable/settings"
        android:text="Button"
        app:layout_constraintBottom_toBottomOf="@+id/host_button"
        app:layout_constraintStart_toEndOf="@+id/host_button"
        app:layout_constraintTop_toTopOf="@+id/host_button" />

</androidx.constraintlayout.widget.ConstraintLayout>